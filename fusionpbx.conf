UserParameter=fusionpbx.extensions.total, export PGPASSWORD="`grep db_password /etc/fusionpbx/config.php | awk -F\' '{print $2,$4}'`"; psql postgresql://fusionpbx:`echo $PGPASSWORD`@localhost:5432/fusionpbx -A -c "select count(*) as cnt  from v_extensions ex where ex.enabled = 'true' " -t
